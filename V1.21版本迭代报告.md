# V1.21 版本迭代报告

## 版本信息
- **版本号**: V1.21
- **发布日期**: 2026-05-14
- **上一版本**: V1.2

---

## 本次迭代内容

### 1. 后端 Bug 修复

#### 1.1 SequencePlanner API Bug 修复
**文件**: `src/api/routes.py`

**问题**: `SequencePlanner()` 未传入 `neo4j_client`，导致拓扑排序端点返回空结果

**修复**:
```python
# 修复前
planner = SequencePlanner()

# 修复后
planner = SequencePlanner(neo4j_client)
```

同时修复了 `components` 参数默认为空列表导致跳过 `_load_components()` 的问题。

#### 1.2 依赖缺失修复
**文件**: `requirements.txt`

添加缺失依赖：
- `networkx==3.2.1` — SequencePlanner 拓扑排序需要
- `structlog==24.1.0` — DepthEvaluator 日志需要
- `duckduckgo-search==6.2.6` — 联网搜索需要

---

### 2. 前端展示优化

#### 2.1 QueryPage Markdown 渲染
**新增文件**:
- `frontend/src/components/MarkdownRenderer.tsx`

使用 `react-markdown` 库渲染 LLM 回答中的 Markdown 格式（粗体、列表等）。

#### 2.2 SequencePlanner 双序列展示
**问题**: 之前只展示 LLM 生成序列

**解决方案**: 同时调用两个 API 并上下展示：
1. **拓扑排序序列**（上）— 基于 Neo4j precedence 规则的确定性排序
2. **LLM 生成序列**（下）— 带推理链和置信度评估

**新增文件**:
- `frontend/src/components/SequenceSection.tsx` — 序列区块容器
- `frontend/src/components/StepCard.tsx` — 步骤卡片组件
- `frontend/src/components/CollapsibleSource.tsx` — 可折叠来源组件（待后续使用）

**修改文件**:
- `frontend/src/components/ReasoningChainPanel.tsx` — 支持单步骤推理链展示
- `frontend/src/pages/SequencePlanner.tsx` — 调用双 API，展示双序列
- `frontend/src/api/client.ts` — 添加 `sequenceApi`

---

### 3. Docker 端口调整

**文件**: `docker-compose.yml`

由于 Windows Hyper-V 端口保留规则，backend 端口从 `8000` 改为 `50060`。

---

## Git 提交记录

| 提交 | 说明 |
|------|------|
| d59e211 | fix: remove unused props causing TypeScript errors |
| 833cfbd | feat(SequencePlanner): add dual sequence display with topological sort |
| 577b710 | feat(SequencePlanner): extend ReasoningChainPanel for single step |
| 9062198 | feat(SequencePlanner): add StepCard component |
| 825e7e9 | feat(SequencePlanner): add SequenceSection component |
| a0be2d1 | feat(QueryPage): use MarkdownRenderer for answer rendering |
| f1ced5a | feat(QueryPage): add CollapsibleSource component |
| d909f4a | feat(QueryPage): add MarkdownRenderer component |

---

## 待优化项

1. **CollapsibleSource** — 来源标注折叠功能已创建但未集成，需要后端返回结构化证据数据
2. **parseSources** — 解析函数已添加但未使用，用于解析回答中的【来源：】标注
3. **Gray zone 阈值校准** — DepthEvaluator 的 0.10/0.65 阈值待数据驱动调优

---

## 验证结果

### 后端 API 测试
```bash
# 拓扑排序端点
curl -X POST http://localhost:50060/api/v1/disassembly/sequence \
  -H "Content-Type: application/json" \
  -d '{"battery_model":"Audi_A3"}'
# 返回: 21 个步骤的正确序列

# LLM 生成端点
curl -X POST http://localhost:50060/api/v1/disassembly/plan \
  -H "Content-Type: application/json" \
  -d '{"battery_model":"Audi_A3","context":[],"debug":true}'
# 返回: 带推理链的 LLM 生成序列
```

### 前端构建
```bash
cd frontend && npm run build
# ✓ built in 2.62s
```

---

## 服务状态

| 服务 | 端口 | 状态 |
|------|------|------|
| Backend API | localhost:50060 | 运行中 |
| Frontend | localhost:9333 | 运行中 |
| Neo4j | localhost:17474/17687 | 运行中 |
| OneAPI | localhost:4567 | 运行中 |
