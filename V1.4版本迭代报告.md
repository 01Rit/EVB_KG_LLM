# V1.4 前端设计统一 + 图谱显示修复 + 序列规划Bug修复

## 版本信息

- **版本号**: V1.4
- **发布日期**: 2026-05-15
- **标签**: v1.4-frontend-unify-graph-fix

---

## 概述

本次迭代完成了三大目标：(1) 全栈前端界面统一为仪表盘设计风格，所有模块保持一致的视觉语言和交互体验；(2) 修复 Neo4j 图层节点显示不全、导入统计不准确的数据层问题；(3) 修复序列规划接口500错误及进度条显示延迟问题。

---

## 变更详情

### 1. 前端设计统一化（核心）

**目标**：将所有页面统一为仪表盘风格的简约设计体系，保持一致的排版、色彩、间距和交互动画。

| 页面 | 变更内容 |
|------|----------|
| `Dashboard.tsx` | 全新设计：动态数字动画、分层分布图、系统状态卡片、最近查询列表、快速入口网格 |
| `QueryPage.tsx` | 统一进度条样式、搜索历史下拉、Debug模式开关、响应式布局 |
| `SequencePlanner.tsx` | 统一进度条布局、电池型号搜索下拉、拓扑+LLM双结果展示、甘特图集成 |
| `ImportManager.tsx` | 三Tab布局（L1/L2/L3）、统一进度卡片、批量导入按钮组 |
| `GraphExplorer.tsx` | 节点筛选+搜索、力导向图全屏模式、节点详情面板 |
| `Settings.tsx` | Tab式分类设置、配置编辑+保存、系统信息展示 |
| `Sidebar.tsx` | 统一导航样式、活跃页面高亮 |
| `index.css` | 910行全新CSS变量体系：统一颜色、阴影、间距、动画、组件样式 |

**设计规范**：
- CSS 变量驱动的主题系统（`--color-*`, `--space-*`, `--radius-*`, `--shadow-*`）
- 卡片式布局（`.card` + `.card-header` 模式）
- 统一进度条组件（`.progress-bar-container`, `.progress-steps`）
- 渐入动画（`slideUp`, `fadeIn`）+ 骨架屏加载态
- 三色层标识系统（L1=蓝色, L2=绿色, L3=橙色）

### 2. 进度条即时显示优化

**问题**：所有进度条在加载时有延迟弹出，用户体验差。

**修复**：
- 进度条容器始终在 `loading` 状态下渲染，不再条件等待第一个事件
- 添加 `indeterminate` 动画类，在没有进度数据时显示无限滚动动画
- `QueryPage`、`SequencePlanner`、`ImportManager` 三端统一实现

| 文件 | 修改 |
|------|------|
| `QueryPage.tsx` | 进度条立即渲染 + 不确定态动画 |
| `SequencePlanner.tsx` | 进度条步骤移到进度条上方，与QueryPage结构一致 |
| `ImportManager.tsx` | 进度卡片立即显示 + 不确定态动画 |
| `index.css` | 新增 `indeterminate` 动画 keyframes |

### 3. 图谱节点显示修复

**问题**：图谱页面只能获取 L1 组件，L2/L3 节点在数据库中存在但未显示。

**根因**：Neo4j 查询使用了 `:Document` 和 `:Term` 标签，但实际数据库节点标签为 `:L2_Document`、`:L2_Entity`、`:L3_Term`。

**修复**：

| 文件 | 修改 |
|------|------|
| `src/api/graph_routes.py` | 所有查询统一使用 `:Component|L2_Document|L2_Entity|L3_Term` 多标签匹配 |
| `src/api/graph_routes.py` | `_map_display_type()` 新增 `L2_Entity` 映射 |
| `src/kg/models.py` | `to_text()` 使用 `.get()` 容错读取 `start/end/type` 或 `source/target/relation_type` |

### 4. 导入统计修复

**问题**：Dashboard 显示 L2 文档数量仅为 10（仅 `:L2_Document`），实际 L2 层有 395 个 `:L2_Entity` 节点未统计。

**修复**：

| 文件 | 修改 |
|------|------|
| `src/api/import_routes.py` | Cypher 查询增加 `OR d:L2_Entity` |
| `frontend/src/pages/Dashboard.tsx` | 标签改为 "L2 文档和实体数量" |

### 5. 序列规划 500 错误修复

**问题**：`POST /api/v1/disassembly/plan` 返回 `500 Internal Server Error`，错误信息 `'start'`。

**根因**：`cross_layer_retriever.py` 构建边时使用 `source/target/relation_type` 作为键名，但 `EvidenceGraph.to_text()` 使用 `edge["start"]/edge["end"]/edge["type"]` 访问，导致 `KeyError: 'start'`。

**修复**：

| 文件 | 修改 |
|------|------|
| `src/graphrag/cross_layer_retriever.py` | 边字典键名改为 `start/end/type/properties`，与系统其他部分一致 |
| `src/kg/models.py` | `to_text()` 增加 `.get()` 容错，支持两种键名格式 |
| `src/api/routes.py` | 错误日志增加 `traceback.format_exc()` 完整堆栈 |

### 6. 组件重构

多个前端组件进行了重构和优化：

| 组件 | 变更 |
|------|------|
| `GanttChart.tsx` | 甘特图视觉优化，并行批次标签，拓扑排序源标记 |
| `ReasoningChainPanel.tsx` | 推理链面板简化，减少冗余代码 |
| `SequenceSection.tsx` | 统一序列展示组件，支持并行组 |
| `StepCard.tsx` | 步骤卡片重构，适应两种数据源 |
| `MarkdownRenderer.tsx` | Markdown 渲染样式优化 |
| `CollapsibleSource.tsx` | 折叠式来源面板 |

---

## 文件变更统计

```
 27 files changed, 2052 insertions(+), 1832 deletions(-)

 前端 (17 files):
   src/ 核心组件        |  910 lines (index.css 全新设计系统)
   src/pages/           |  6 pages 全面重构
   src/components/      |  6 components 重构优化

 后端 (4 files):
   src/api/graph_routes.py       |  Neo4j 标签统一
   src/api/import_routes.py      |  L2_Entity 计数
   src/api/routes.py             |  traceback 日志
   src/graphrag/cross_layer_retriever.py | 边键名修复
   src/kg/models.py              | to_text() 容错
```

---

## 性能指标

| 指标 | V1.3 | V1.4 |
|------|------|------|
| 图谱节点显示 | L1 仅 11 个 | L1=11 + L2=405 + L3=177 = 593 |
| 导入统计 (documents) | 10 (仅L2_Document) | 456 (L2_Document + L2_Entity) |
| 进度条响应 | 有延迟 (等待首个事件) | 即时显示 + 不确定态动画 |
| 序列规划API | 500 'start' 错误 | 正常返回 5+ steps |
| 前端设计风格 | 各页面不一致 | 统一仪表盘风格 |

---

## 测试验证

1. **图谱节点**：`GET /api/v1/graph/nodes` → 返回 593 节点（L1=11, L2=405, L3=177）
2. **导入统计**：`GET /api/v1/import/status` → `{components: 11, documents: 456, terms: 197}`
3. **序列规划**：`POST /api/v1/disassembly/plan` → `{code: 0, steps: 5, parallel_batches: 4}`
4. **健康检查**：`GET /api/v1/health` → `{status: "healthy"}`
5. **Docker 构建**：`docker-compose up -d --build` 无错误
