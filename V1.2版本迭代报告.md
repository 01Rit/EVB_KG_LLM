# V1.2 版本迭代报告

**版本**: V1.2
**日期**: 2026-05-13
**状态**: 已完成

---

## 一、本次迭代目标

解决 LLM 推理反馈增强 Phase 1-4 实施后的遗留问题，确保新功能在生产环境中正常运行。

---

## 二、解决的问题

### 2.1 EvidenceGraph 属性访问错误

**问题**: `DepthEvaluator` 和 `FeedbackLoop` 中直接访问 `evidence.l1_nodes` / `evidence.l2_nodes` / `evidence.l3_nodes`，但 `EvidenceGraph` 只有扁平的 `nodes` 列表。

**修复**:
- `src/graphrag/depth_evaluator.py` — 改为根据 `node_type` 过滤各层节点
- `src/graphrag/feedback.py` — 同上，`_calc_confidence_factors` 按 `node_type` 分类

**影响文件**: `depth_evaluator.py`, `feedback.py`

---

### 2.2 planner.py 变量未定义

**问题**: `trace['iteration_count'] = iterations` 中 `iterations` 未定义，应为 `len(reasoning_traces)`。

**修复**: `src/graphrag/planner.py:280`

---

### 2.3 Config 加载问题

**问题**:
1. `config.py` 中 `load_dotenv()` 未被调用，`.env` 文件配置无法加载
2. `MAX_TOKENS` 未从环境变量读取

**修复**:
- `src/config.py` — 添加 `from dotenv import load_dotenv` 和 `load_dotenv()`
- `src/config.py` — `load_settings()` 中添加 `max_tokens=int(os.getenv('MAX_TOKENS', '2000'))`

---

### 2.4 LLM API 配置问题

**问题**: `.env` 中 `OPENAI_BASE_URL` 指向 `https://api.openai.com/v1`，无法直接访问。

**修复** (`.env`):
```
OPENAI_BASE_URL=http://localhost:4567/v1
LLM_MODEL=deepseek-chat
MAX_TOKENS=4000
```

---

### 2.5 Generator JSON 输出截断

**问题**: `max_tokens=2000` 导致 LLM 响应被截断，JSON 解析失败，`steps` 为空。

**修复**:
- `.env` 中设置 `MAX_TOKENS=4000`
- `config.py` 中读取 `MAX_TOKENS` 环境变量

---

## 三、新增功能验证

### 3.1 API 新增字段

| 字段 | 描述 | 验证状态 |
|------|------|---------|
| `reasoning_traces` | 迭代级推理追踪列表 | ✅ |
| `total_feedback_iterations` | 总反馈迭代次数 | ✅ |
| `final_confidence` | 最终置信度 | ✅ |
| `steps[].reasoning_chain` | 步骤级结构化推理链 | ✅ |

### 3.2 跨层检索

| 指标 | 值 |
|------|---|
| `cross_layer_triggered` | True |
| `cross_layer_nodes_added` | 25 |

### 3.3 置信度评估

- 3次迭代，depth=2（全链路）
- 最终置信度 0.25 (FAIL_COVERAGE)

---

## 四、配置变更

### 4.1 .env

```diff
  NEO4J_URI=bolt://localhost:17687
  NEO4J_USER=neo4j
  NEO4J_PASSWORD=88888888
  OPENAI_API_KEY=sk-GNFFv3jfsoVCDDWCA15875E0De4140B99c3aF15a7400422b
- OPENAI_BASE_URL=https://api.openai.com/v1
+ OPENAI_BASE_URL=http://localhost:4567/v1
+ LLM_MODEL=deepseek-chat
+ MAX_TOKENS=4000
  MILVUS_HOST=localhost
  MILVUS_PORT=19530
```

### 4.2 模型配置

- **LLM**: deepseek-chat (通过 OneAPI 中转)
- **OneAPI 端口**: 4567

---

## 五、待优化项

1. **`confidence_info` 为 None** — `ConfidenceInfo` 由 feedback loop 评估后填充，planner 层面需传递该信息至 steps
2. **灰色地带阈值未校准** — 0.10 < coverage < 0.65 的初始值待 10 条标注数据调优
3. **LLM 重试逻辑** — 当前无重试，超时直接失败

---

## 六、测试验证

### 6.1 单元测试
```bash
python -m pytest tests/graphrag/ -v
# 结果: 33 passed, 1 skipped
```

### 6.2 API 测试
```bash
curl -X POST http://localhost:8001/api/v1/disassembly/plan \
  -H 'Content-Type: application/json' \
  -d '{"battery_model":"Audi_A3","context":[],"debug":true}'
```
- steps: 11
- reasoning_traces: 3
- final_confidence: 0.25
- reasoning_chain: 正常返回

---

## 七、版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| V1.0 | 2026-04 | 初始版本，三层知识图谱 + GraphRAG |
| V1.1 | 2026-05 | LLM 推理反馈增强 Phase 1-4 |
| V1.2 | 2026-05-13 | Bug 修复 + OneAPI 配置 + MAX_TOKENS 调优 |
