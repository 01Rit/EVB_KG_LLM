# V1.5 — 联网搜索修复：DuckDuckGo → Serper API + 前端来源展示

**发布日期**: 2026-05-19 | **标签**: `v1.5-web-search-fix`

---

## 概述

本次迭代解决了联网搜索功能在 Docker 容器内不可用的根本问题，同时补全了前端来源展示功能（"参考来源"区域始终为空的白屏问题）。

## 问题背景

### 联网搜索不可用

当前使用 `duckduckgo-search==6.2.6` 作为搜索源，但在 Docker 容器内 SSL 握手失败（`SSLEOFError`），无法连接 DuckDuckGo 及其代理的 Bing 搜索 API。宿主机上该库的新版本也通过 Bing 代理查询，同样不可用。**联网搜索功能在国内 Docker 环境彻底不可用。**

### 前端来源不显示

`QueryPage.tsx` 中 `sources` state 从未被 SSE "done" 事件填充，导致"参考来源"区域永远隐藏。已有的 `CollapsibleSource` 组件处于废弃状态。

## 变更详情

### 1. 搜索源替换：DuckDuckGo → Serper API

Serper API 是 Google 搜索的 HTTP 代理服务，国内可访问，无需 OAuth，免费额度 2500 次/月。

**涉及文件**：

| 文件 | 变更 |
|------|------|
| `src/graphrag/web_searcher.py` | 移除 DuckDuckGo 导入，改用 `requests.post("https://google.serper.dev/search")`，参数 `gl=cn, hl=zh-cn`，`verify_ssl=False`（适配 Docker 网络环境） |
| `src/config.py` | `Settings` 类新增 `serper_api_key: str = ""` |
| `.env` | 新增 `SERPER_API_KEY=...` |
| `docker-compose.yml` | backend 环境变量新增 `SERPER_API_KEY=${SERPER_API_KEY}` |
| `src/api/query_routes.py` | 传递 `serper_api_key` 参数到 `NaturalLanguageFeedback` |

**请求格式**：

```python
POST https://google.serper.dev/search
Headers: X-API-KEY, Content-Type: application/json
Body: {"q": "搜索词", "gl": "cn", "hl": "zh-cn"}
```

**响应字段映射**：
- `organic[].title` → `title`
- `organic[].link` → `url`
- `organic[].snippet` → `snippet`

### 2. 前端来源展示

**涉及文件**：

| 文件 | 变更 |
|------|------|
| `src/graphrag/natural_feedback.py` | SSE "done" 事件增加 `sources` 数组，含本地KG和联网搜索的结构化来源（type/name/snippet/url） |
| `src/api/query_routes.py` | 同步端点改用结构化 sources（替代原 regex 提取） |
| `frontend/src/pages/QueryPage.tsx` | 从 SSE 解析 `sources`，用 `CollapsibleSource` 渲染可展开来源列表 |
| `frontend/src/components/CollapsibleSource.tsx` | 增加 `url` 字段支持，联网搜索结果显示"查看原文"链接 |

## 验证

### 搜索功能验证（容器内）

```
WebSearcher: found 3 results for query=电池拆卸步骤
  → 汽车电瓶拆安装顺序，拆装汽车电瓶的步骤 - 汽车之家
  → 动力电池拆卸步骤百科 - 新浪
  → 车上电瓶怎么拆 - 中华网
```

### API 响应验证

```json
{
  "code": 0,
  "data": {
    "answer": "...",
    "sources": [
      {"type": "本地KG-Document", "name": "GBT+34015.2-2020...", "snippet": "..."},
      {"type": "联网搜索", "name": "Battery Disassembly...", "snippet": "...", "url": "..."}
    ]
  }
}
```

同步 API 验证：9 条本地KG + 5 条联网搜索结果，来源结构完整。

## 后端逻辑变更摘要

```diff
- DuckDuckGo DDGS.text()
+ Serper API POST google.serper.dev/search

- sources state never populated
+ SSE done 事件包含 sources 数组

- CollapsibleSource 废弃
+ 问答页面渲染可展开来源列表
```

## 影响范围

| 影响 | 说明 |
|------|------|
| 联网搜索 | 从不可用 → 正常工作（Google 搜索结果） |
| 前端来源区 | 从永远隐藏 → 显示可展开的来源列表 |
| API 兼容性 | SSE "done" 事件新增 `sources` 字段，向后兼容 |

## 完整文件变更清单

| 文件 | 变更类型 |
|------|----------|
| `src/graphrag/web_searcher.py` | 重写 |
| `src/graphrag/natural_feedback.py` | 修改 |
| `src/config.py` | 修改 |
| `src/api/query_routes.py` | 修改 |
| `.env` | 修改 |
| `docker-compose.yml` | 修改 |
| `backend/Dockerfile` | 修改（版本标签） |
| `frontend/src/pages/QueryPage.tsx` | 修改 |
| `frontend/src/components/CollapsibleSource.tsx` | 修改 |
| `v1.5版本迭代报告.md` | 新增 |
