# V2.2 — L1 组件再制造评价属性 + 评价子图增强

**发布日期**: 2026-05-21 | **标签**: `v2.2-eval-attributes-subgraph`
**前序版本**: V2.1 (模糊匹配 + 多维评价体系)

---

## 概述

V2.1 实现了模糊匹配 + 多维评价体系，但评价规则匹配时**子图数据缺失**——组件节点没有再制造属性，规则匹配分数始终为 0。本次迭代解决这一核心问题：

1. **15 项再制造评价属性**：新增 `ComponentEvalAttributes` 模型，覆盖模块化形式、连接方式、工具需求、安全风险等 15 个维度
2. **LLM 自动提取**：L1 导入时 LLM 从源文档自动提取属性，存为 Component 节点的 `eval_attributes` JSON
3. **评价子图增强**：`_build_subgraph` 从 Neo4j 查询组件真实名称 + eval_attributes，替代原来的 ID 作为名称
4. **属性匹配回退**：规则匹配失败时，检查节点 `eval_attributes` 字段进行模糊匹配
5. **规则持久化**：L4Rule + L4RuleCondition 持久化到 Neo4j，服务重启不丢失
6. **前端属性编辑器**：创建版本时可查看/编辑每个组件的 15 项属性

从"子图无属性、规则匹配为 0"升级为"属性自动提取 + 属性匹配评分"：

```
V2.1 (子图无属性):
  组件节点: {id, name=ID}  ← name 用 ID 代替，无属性
  规则匹配: 关系匹配 → 失败 → 评分 0

V2.2 (属性驱动评价):
  组件节点: {id, name=真实名称, eval_attributes={15项}}
  L1导入 → LLM提取属性 → SET c.eval_attributes = JSON
  规则匹配:
    1. 关系匹配(RELATES) → 成功 → 1.0
    2. 关系匹配失败 → eval_attributes匹配 → 成功 → 0~1
    3. 均失败 → 0.0
```

---

## 架构变更

### 属性提取流程

```
L1 导入 (TXT/PDF/Markdown)
    │
    ├── EntityExtractor.extract_triplets(text) → 三元组
    │
    ├── 创建 Component 节点 (MERGE)
    │
    └── _extract_eval_attributes(name, text, neo4j)
         │
         ├── LLM Prompt: 从文本提取 15 项属性 → JSON
         │
         └── SET c.eval_attributes = JSON字符串
```

### 评价子图构建

```
V2.1:
  _build_subgraph(component_ids, connection_ids)
    → nodes: [{id, labels=["L1_Component"], name=ID}]  ← name 用 ID
    → 无 eval_attributes
    → connection_ids 当作节点名处理

V2.2:
  _build_subgraph(component_ids, connection_ids)
    → 查询 Neo4j: MATCH (c:Component {id}) RETURN c.name, c.eval_attributes
    → nodes: [{id, labels=["L1_Component"], name=真实名称, eval_attributes={...}}]
    → connection_ids 解析 "from->to" 格式，查询 RELATES 关系属性
```

### 规则匹配增强

```
V2.1:
  _match_condition(condition, subgraph)
    → 关系匹配(RELATES) → 成功/失败
    → 无属性匹配回退

V2.2:
  _match_condition(condition, subgraph)
    → 关系匹配(RELATES) → 成功 → 1.0
    → 失败 → 属性匹配(eval_attributes)
       ├── REQUIRES_CONNECTION → connection_type
       ├── REQUIRES_TOOL → tool_requirements
       ├── REQUIRES_STRUCTURE → accessibility
       └── CONSTRAINED_BY → safety_risks
    → 子串精确匹配 → 1.0
    → FuzzyScorer 模糊匹配 → 0~1
    → 均失败 → 0.0
```

---

## 15 项再制造评价属性

| 属性 | 字段名 | 说明 | 示例值 |
|------|--------|------|--------|
| 模块化形式 | `modularity` | 电池系统模块化架构 | CTP / CTM / MTP / CTC |
| 连接方式 | `connection_type` | 部件间连接方法 | M6六角螺栓连接 / 激光焊接 |
| 连接可逆性 | `connection_reversibility` | 连接拆装特性 | 可重复拆装5次 / 拆卸需破坏胶层 |
| 所需工具 | `tool_requirements` | 拆卸所需专用工具 | T20 Torx螺丝刀 / 电动绝缘扳手 |
| 可达性 | `accessibility` | 部件操作空间 | 需移除上盖后触达 / 位于Pack底部 |
| 安全风险 | `safety_risks` | 拆卸风险源 | 800V高压母排 / 电解液泄漏风险 |
| 材料类型 | `material_type` | 部件主体材料 | 6061铝合金 / PA66+GF30 |
| 拆卸工时 | `estimated_time` | 典型拆卸耗时 | 单人拆卸约8 min |
| 再利用潜力 | `reusability` | 再制造/梯次利用前景 | 可直接用于梯次储能 |
| 检测方法 | `inspection_method` | 再制造前检测手段 | 红外热像检测 / 超声探伤 |
| 密封类型 | `seal_type` | 密封结构与材料 | 双组分环氧密封胶 / IP67硅胶圈 |
| 拆卸顺序 | `disassembly_order` | 顺序约束 | 需先断开高压回路 |
| 再组装扭矩 | `reattachment_torque` | 扭矩规范 | M8螺栓18 N·m |
| 故障清除 | `fault_clearing` | 软件处理要求 | 需使用OEM诊断仪重置BMS |
| 危险物质 | `hazardous_material` | 危险介质/污染源 | 冷却液含乙二醇 / 电解液含LiPF6 |

**存储格式**: Component 节点属性 `eval_attributes`，JSON 字符串

```json
{
  "modularity": "CTP",
  "connection_type": "M6六角螺栓连接",
  "tool_requirements": "T20 Torx螺丝刀 + 10mm套筒",
  "safety_risks": "800V高压母排，需先断开MSD",
  ...
}
```

---

## 变更详情

### 1. 数据模型 (`src/evaluation/models.py`)

**新增模型**:

```python
class ComponentEvalAttributes(BaseModel):
    """15 项再制造评价属性"""
    modularity: str = ""
    connection_type: str = ""
    connection_reversibility: str = ""
    tool_requirements: str = ""
    accessibility: str = ""
    safety_risks: str = ""
    material_type: str = ""
    estimated_time: str = ""
    reusability: str = ""
    inspection_method: str = ""
    seal_type: str = ""
    disassembly_order: str = ""
    reattachment_torque: str = ""
    fault_clearing: str = ""
    hazardous_material: str = ""
```

### 2. 评价子图增强 (`src/evaluation/version_manager.py`)

**重写 `_build_subgraph`**:

- 从 Neo4j 查询组件真实名称和 `eval_attributes`
- `connection_ids` 解析 `"from->to"` 格式，查询 RELATES 关系属性
- 组件名称从 ID 改为 Neo4j 中的 `c.name`
- 新增 `eval_attributes` 字段（JSON 解析后的 dict）

```python
def _build_subgraph(self, component_ids, connection_ids):
    nodes = []
    for cid in component_ids:
        rows = self.neo4j.execute_query(
            "MATCH (c:Component {id: $id}) RETURN c.name AS name, c.eval_attributes AS attrs",
            {"id": cid}
        )
        if rows:
            attrs = json.loads(rows[0]["attrs"] or "{}")
            nodes.append({
                "id": cid, "labels": ["L1_Component"],
                "name": rows[0]["name"], "eval_attributes": attrs,
            })
    # connection_ids: "from->to" → 查询 RELATES 关系
    ...
```

### 3. 规则匹配属性回退 (`src/evaluation/rule_engine.py`)

**新增属性匹配逻辑**:

在 `_match_condition` 中，关系匹配失败后，检查节点 `eval_attributes`：

```python
attr_field_map = {
    "REQUIRES_CONNECTION": "connection_type",
    "REQUIRES_TOOL": "tool_requirements",
    "REQUIRES_STRUCTURE": "accessibility",
    "CONSTRAINED_BY": "safety_risks",
}

# 关系匹配失败后
attr_field = attr_field_map.get(condition.condition_type)
if attr_field:
    for node in subgraph.get("nodes", []):
        attr_value = node.get("eval_attributes", {}).get(attr_field, "")
        if target_name.lower() in attr_value.lower():
            return 1.0  # 子串精确匹配
        if self.fuzzy_scorer:
            return self.fuzzy_scorer.score(...)  # 模糊匹配
```

### 4. 规则 Neo4j 持久化 (`src/evaluation/rule_engine.py`)

| 方法 | 说明 |
|------|------|
| `_load_from_neo4j()` | 启动时加载所有 L4Rule + L4RuleCondition |
| `_persist_rule(rule)` | MERGE rule → DELETE 旧 conditions → CREATE 新 conditions |
| `_remove_rule(rule_id)` | DELETE rule + conditions |

**Neo4j 数据模型**:
```
(r:L4Rule {rule_id, name, conclusion_score, ...})
  -[:HAS_CONDITION {condition_index}]->
(c:L4RuleCondition {condition_type, target_label, ...})
```

### 5. LLM 属性提取 (`src/api/import_routes.py`)

**新增函数** `_extract_eval_attributes`:

- L1 导入后（TXT/PDF/Markdown），对每个新组件调用 LLM
- Prompt 包含 15 项属性定义 + 示例 + 源文本
- LLM 返回纯 JSON → 解析 → `SET c.eval_attributes = JSON`
- 容错：文本过短跳过、JSON 解析失败静默降级

**集成点**:

| 导入方式 | 集成位置 |
|---------|---------|
| `import_l1_txt` | 节点创建后、评分前 |
| `import_l1_pdf` | 节点创建后 |
| `import_l1_markdown` | 节点创建后 |
| `import_l1_csv` | 未集成（结构化数据，无源文本） |
| `import_l1_manual` | 未集成（无源文本） |

### 6. 组件属性 API (`src/api/eval_routes.py`)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/evaluation/components/{id}/eval-attributes` | 获取组件属性 |
| PUT | `/api/v1/evaluation/components/{id}/eval-attributes` | 更新组件属性 |

### 7. 前端属性编辑器 (`frontend/src/pages/EvaluationPage.tsx`)

**版本创建模态框新增**:

- 组件选择器下方显示"组件评价属性"区域
- 每个已选组件旁有"编辑属性"按钮
- 点击打开属性编辑弹窗（15 个输入框，2 列布局）
- 保存调用 `PUT /api/v1/evaluation/components/{id}/eval-attributes`

**前端 API** (`frontend/src/api/client.ts`):

```typescript
getEvalAttributes: (componentId: string) =>
  api.get(`/evaluation/components/${componentId}/eval-attributes`),
updateEvalAttributes: (componentId: string, data: Record<string, string>) =>
  api.put(`/evaluation/components/${componentId}/eval-attributes`, data),
```

### 8. Bug 修复

| 问题 | 修复 |
|------|------|
| Candidate ID undefined | 前端 `candidate_id` → `rule_id`，与后端 CandidateRule 模型对齐 |
| approve_candidate 规则不入库 | 改用 `create_rule(L4RuleCreate(...))` 替代直接 `_rules` 字典赋值 |
| 版本创建连接列表不合理 | 过滤为仅显示 L1 组件间关系 |
| PDF 导入创建 Entity 节点 | 改为创建 Component 节点，与 TXT/Markdown 一致 |

---

## 文件变更清单

### 后端

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `src/evaluation/models.py` | 修改 | 新增 `ComponentEvalAttributes` 模型（15 字段） |
| `src/evaluation/version_manager.py` | 修改 | 重写 `_build_subgraph`，查询 Neo4j 组件名称+属性+关系 |
| `src/evaluation/rule_engine.py` | 修改 | 新增 `_load_from_neo4j`/`_persist_rule`/`_remove_rule`；`_match_condition` 属性匹配回退 |
| `src/api/eval_routes.py` | 修改 | 新增 GET/PUT 组件属性接口 |
| `src/api/import_routes.py` | 修改 | 新增 `_extract_eval_attributes`；L1 TXT/PDF/Markdown 导入后自动提取属性 |

### 前端

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `frontend/src/api/client.ts` | 修改 | 新增 `getEvalAttributes`/`updateEvalAttributes` |
| `frontend/src/pages/EvaluationPage.tsx` | 修改 | 属性编辑器（选择组件→编辑属性弹窗→15 项输入） |

### 测试

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `tests/evaluation/test_version_manager.py` | 修改 | Mock 返回组件名称+属性，适配新 `_build_subgraph` |
| `tests/evaluation/test_rule_engine.py` | 修改 | 新增 `TestPersistence` 类（4 个测试），Mock 适配 Neo4j 加载 |

---

## 测试覆盖

**总计 233 个评价模块测试全部通过**：

| 测试文件 | 数量 | 覆盖范围 |
|---------|------|---------|
| test_models.py | 71 | 数据模型、枚举、字段验证 |
| test_fuzzy_scorer.py | 6 | 模糊评分、缓存、降级 |
| test_rule_engine.py | 23 | 规则匹配（精确+模糊）、Neo4j 持久化 |
| test_evaluator.py | 24 | 维度分组、批量 RSR、等级映射 |
| test_rsr.py | 6 | RSR 编秩、递阶、动态分档 |
| test_version_manager.py | 13 | 版本管理、子图构建、属性读取 |
| test_import_handler.py | 30 | 规则提取、标准化、去重 |
| test_integration.py | 13 | 端到端闭环、多维评价、批量评价 |
| 其他（closed_loop, action_executor 等） | 47 | 已有功能回归 |

**新增测试**:

| 测试类 | 测试点 |
|--------|--------|
| `TestPersistence::test_create_persists_to_neo4j` | 创建规则时调用 `_persist_rule` |
| `TestPersistence::test_delete_removes_from_neo4j` | 删除规则时调用 `_remove_rule` |
| `TestPersistence::test_update_persists_to_neo4j` | 更新规则时调用 `_persist_rule` |
| `TestPersistence::test_load_from_neo4j` | 启动时从 Neo4j 加载规则 |
| `TestGetSubgraph::test_returns_correct_nodes` | 节点含 `name`（来自 Neo4j）和 `eval_attributes` |
| `TestDiffVersions::test_detects_added_and_removed_rels` | 关系 diff 正确处理 |

---

## Git 提交记录

```
4856e38 feat(evaluation): add L1 component eval attributes + subgraph enrichment
76fcfca feat(evaluation): add select-all buttons for components and connections
4878d5e fix(evaluation): filter connections to L1-only in version creation form
aae1e01 feat(evaluation): add version creation form with graph component/connection selector
3edd47b feat(evaluation): persist L4 rules to Neo4j with L4RuleCondition nodes
1869c84 fix(evaluation): Candidate ID field name mismatch (candidate_id → rule_id)
```

---

## 向后兼容

- `ComponentEvalAttributes` 所有字段默认空字符串，无属性时不影响评价
- `eval_attributes` 为空时，规则匹配回退到原有关系匹配逻辑
- 规则持久化兼容：启动时自动从 Neo4j 加载，已有内存规则不受影响
- Component 节点无 `eval_attributes` 时，`_build_subgraph` 返回空 dict `{}`

---

## 已知限制

1. **CSV/手动导入无属性提取**：结构化数据无源文本，需手动通过 API 或前端编辑
2. **属性提取依赖 LLM**：每个组件导入时调用一次 LLM，大量组件导入较慢
3. **属性匹配仅 4 个字段**：`connection_type`/`tool_requirements`/`accessibility`/`safety_risks`，其余 11 个属性暂不参与规则匹配
4. **属性编辑无批量操作**：前端仅支持逐个组件编辑

---

## 后续迭代方向

- [ ] 属性提取支持 CSV 导入（从列名映射）
- [ ] 全部 15 个属性字段参与规则匹配
- [ ] 批量属性编辑（多组件同时编辑）
- [ ] 属性变更历史追踪
- [ ] 属性模板（预设常见电池组件属性组合）
- [ ] 属性缺失提醒（评价前检查属性完整度）
