# V2.1 — 模糊匹配 + 多维评价体系

**发布日期**: 2026-05-21 | **标签**: `v2.1-fuzzy-multidimensional-evaluation`
**参考论文**: 张秀芬, 高云飞. 退役机械零部件多维递阶再制造性评价方法. 浙江大学学报, 2020.

---

## 概述

本次迭代在 V2.0 L4 评价层基础上，将**二值匹配评价系统**升级为**模糊匹配 + 多维评价体系**。核心改进：

1. **模糊规则匹配**：LLM 驱动的连续评分（0~1），替代原来的精确匹配（True/False）
2. **多维评价体系**：技术/经济/环境三维度，与论文 22 指标体系对齐
3. **RSR 递阶合成**：批量评价时使用分段加权秩和比法，支持动态分档
4. **4 级等级制**：优秀/良好/合格/不可再制造，替代原来的高/中/低三级

从"二值匹配 + 单一评分"升级为"模糊匹配 + 多维递阶评价"：

```
V2.0:
  规则匹配 → True/False → 加权平均 → 高/中/低

V2.1:
  规则匹配 → 模糊分数(0~1) → 按维度分组 → 维度加权平均 → 4级等级
  批量模式: 规则分数 → 维度RSR → 递阶合成 → 动态分档(Probit回归)
```

---

## 架构变更

### 评价流程对比

```
V2.0 (二值匹配):
  subgraph ──→ RuleEngine.match_rule() ──→ bool
                 ↓ matched=True
              conclusion_score × weight ──→ 加权平均 ──→ 高/中/低

V2.1 (模糊匹配 + 多维):
  subgraph ──→ RuleEngine.match_rule() ──→ float(0~1)
                 │                           ↓ 精确匹配=1.0
                 │                           ↓ 模糊匹配=FuzzyScorer(LLM)
                 │                           ↓ 不匹配=0.0
                 ↓
              按Dimension分组
                 ├── technical:  dim_score = Σ(score×weight) / Σ(weight)
                 ├── economic:   dim_score = ...
                 └── environmental: dim_score = ...
                 ↓
              overall = Σ(dim_score × β_z) / Σ(β_z)
                 ↓
              GradeConfig阈值 → 优秀/良好/合格/不可再制造

  批量模式:
              各版本各维度分数 → 维度内编秩 → RSR_z
              → 维度间递阶 → 总RSR → Probit回归 → 动态分档阈值
```

### 新增模块

```
src/evaluation/
├── fuzzy_scorer.py    # LLM 模糊评分器 + LRU 缓存
├── rsr.py             # RSR 编秩、递阶合成、动态分档
└── ...

frontend/src/components/
├── RadarChart.tsx       # SVG 三维雷达图
├── GradeConfigPanel.tsx # 阈值配置面板
└── ...
```

---

## 变更详情

### 1. 数据模型扩展 (`src/evaluation/models.py`)

**新增枚举**：

| 枚举 | 值 | 说明 |
|------|-----|------|
| `Dimension` | technical / economic / environmental | 评价维度 |

**修改枚举**：

| 枚举 | V2.0 | V2.1 |
|------|------|------|
| `Grade` | 高 / 中 / 低 | 优秀 / 良好 / 合格 / 不可再制造 |

**新增模型**：

| 模型 | 说明 |
|------|------|
| `DimensionScore` | 维度评分（分数/RSR值/秩/等级/匹配数） |
| `GradeThreshold` | 动态分档阈值（excellent/good/qualified + 回归参数） |
| `GradeConfig` | 等级阈值配置（可标定，来源标记） |

**扩展字段**：

| 模型 | 新增字段 |
|------|---------|
| `L4RuleCondition` | `fuzzy_threshold: float = 0.6` |
| `L4Rule` | `dimension: Dimension`, `fuzzy_threshold: float = 0.6` |
| `L4RuleCreate` | `dimension: Dimension` |
| `L4Assessment` | `dimension_scores`, `evaluation_mode`, `dimension_weights`, `grade_thresholds`, `rank_matrix`, `per_version` |
| `CandidateRule` | `dimension`, `fuzzy_threshold`, `duplicate_status`, `duplicate_of` |

### 2. 模糊评分器 (`src/evaluation/fuzzy_scorer.py`, 新增)

LLM 驱动的条件模糊匹配，替代二值判断：

- **精确匹配短路**：名称完全相同 → 直接返回 1.0，不调用 LLM
- **模糊评分**：精确匹配失败时，调用 LLM 评估实体满足条件的程度（0~1）
- **LRU 缓存**：key 为 `(condition_type, target_label, actual_name)`，避免重复调用
- **评分 Prompt**：包含规则信息（名称、描述、维度、结论分数）、条件详情、实体完整信息、维度特定评分指导
- **容错**：LLM 返回异常时降级为 0.0

```python
class FuzzyScorer:
    def score(self, condition_type, target_label, actual_name, ...) -> float:
        # 1. 缓存命中 → 返回缓存值
        # 2. 精确匹配 → 1.0
        # 3. LLM 评分 → 0.0~1.0（带 clamp）
```

### 3. RuleEngine 模糊匹配改造 (`src/evaluation/rule_engine.py`)

- `match_rule` 返回类型从 `tuple[bool, list]` 改为 `tuple[float, list]`
- `_match_condition` 返回 `float`：精确匹配=1.0，模糊匹配=0~1，不匹配=0.0
- 规则总分 = 条件分数的加权平均，匹配阈值 = `rule.fuzzy_threshold`
- 向后兼容：无 FuzzyScorer 时，精确匹配仍返回 1.0，不匹配返回 0.0

### 4. RSR 计算模块 (`src/evaluation/rsr.py`, 新增)

实现论文中的秩和比法：

| 函数 | 说明 |
|------|------|
| `compute_dimension_rsr(scores, weights)` | 维度内 RSR_z：按规则编秩 → 加权秩和比 |
| `compute_total_rsr(dim_rsrs, dim_weights)` | 维度间递阶：对 3 维度 RSR 再编秩 → 总 RSR |
| `compute_dynamic_thresholds(total_rsrs)` | 动态分档：累计频率 → Probit 变换(P=Φ⁻¹(p)+5) → 线性回归 |

**关键公式**：
- 累计频率校正：`p_l = 1 - 1/(4l)`（最后一个对象，避免 Probit 无穷大）
- 概率单位：`P = Φ⁻¹(p) + 5`（+5 偏移，使 P 在 [2,8] 范围）
- 分档阈值：优秀 P≥6.5，良好 P≥5.0，合格 P≥3.5

### 5. Evaluator 多维评价改造 (`src/evaluation/evaluator.py`)

**单版本模式**：
- 按 `Dimension` 分组规则，分别调用 `_evaluate_dimension`
- 维度分数 = `Σ(rule_score × rule_weight) / Σ(rule_weight)`
- 总分 = `Σ(dim_score × dim_weight) / Σ(dim_weight)`
- 等级由 `GradeConfig` 阈值决定（可配置、可标定）

**批量模式** (`batch_evaluate`)：
- 收集各版本各规则的匹配分数（一次性计算，避免重复调用）
- 按维度计算 RSR → 递阶合成总 RSR → 动态分档阈值
- 每个维度独立计算阈值（不与总分共用）
- 返回 `evaluation_mode="batch"` 的 `L4Assessment` 列表

### 6. 规则提取改进 (`src/evaluation/import_handler.py`)

**EXTRACTION_PROMPT 升级**：
- 包含 22 指标 × 3 维度的完整定义
  - 技术维度 (8 项)：拆卸难度、连接方式、工具需求、结构可达性、清洗难度、检测复杂度、修复方案可行性、装配精度要求
  - 经济维度 (7 项)：拆卸时间、人力成本、工具/设备成本、零部件回收价值、场地/耗材费用、再制造加工成本、再制造产品售价
  - 环境维度 (7 项)：零部件充足程度、废料产生量、污染风险、资源回收率、能源消耗、政策合规性、客户接受度
- 2 个 few-shot 示例（螺栓连接、焊接连接）
- 要求输出 `dimension` 和 `fuzzy_threshold` 字段

**标准化评分** (`_standardize_score`)：
- 提取完成后，调用第二次 LLM 对比已有规则库评分标准
- 统一评分尺度，输出调整后的 score/grade/weight

**去重逻辑** (`check_duplicates`)：
- Level 1：精确匹配（rule name 完全相同）
- Level 2：条件重叠（相同 condition_type + target_label）

### 7. API 路由扩展 (`src/api/eval_routes.py`)

**新增接口**：

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/evaluation/batch-assess` | 批量评价（RSR 递阶合成） |
| GET | `/api/v1/evaluation/grade-config` | 获取阈值配置 |
| PUT | `/api/v1/evaluation/grade-config` | 更新阈值配置 |
| POST | `/api/v1/evaluation/grade-config/calibrate` | 自动标定（需≥10条历史数据） |

**修改接口**：

| 方法 | 路径 | 变更 |
|------|------|------|
| POST | `/api/v1/evaluation/rules` | 增加 `dimension`, `fuzzy_threshold` 参数 |
| PUT | `/api/v1/evaluation/rules/{id}` | 同上 |

### 8. 前端改造

#### Tab 1 评价看板

| 功能 | 说明 |
|------|------|
| 雷达图 | SVG 三维雷达图，展示技术/经济/环境三维度分数 |
| 维度详情表 | 维度、评分、等级、命中规则数 |
| 批量评价 | 多选版本 → 调用 batchAssess API → 展示 RSR 结果 |
| 4 级等级 | 优秀(绿)/良好(蓝)/合格(黄)/不可再制造(红) |
| 阈值配置 | GradeConfigPanel 集成，支持手动调整和自动标定 |

#### Tab 3 规则库管理

| 功能 | 说明 |
|------|------|
| 维度列 | 彩色标签（技术=蓝/经济=绿/环境=紫） |
| 模糊阈值列 | 显示各规则的 fuzzy_threshold |
| 创建/编辑表单 | 新增维度下拉框、模糊阈值滑块(0~1, step 0.05) |
| 等级选项 | 优秀/良好/合格/不可再制造 |

#### 新增组件

| 组件 | 说明 |
|------|------|
| `RadarChart.tsx` | SVG 雷达图，支持自定义大小和数据 |
| `GradeConfigPanel.tsx` | 阈值配置面板，含手动调整和自动标定 |

---

## 文件变更清单

### 后端

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `src/evaluation/models.py` | 修改 | 新增 Dimension, DimensionScore, GradeConfig；Grade 改 4 级 |
| `src/evaluation/fuzzy_scorer.py` | **新增** | LLM 模糊评分 + 缓存 |
| `src/evaluation/rule_engine.py` | 修改 | match_rule 返回 float，集成 FuzzyScorer |
| `src/evaluation/evaluator.py` | 修改 | 维度分组、_evaluate_dimension、batch_evaluate |
| `src/evaluation/rsr.py` | **新增** | RSR 编秩、递阶合成、动态分档 |
| `src/evaluation/import_handler.py` | 修改 | 22 指标 prompt、标准化评分、去重 |
| `src/evaluation/closed_loop.py` | 修改 | batch_assess、grade_config CRUD、标定 |
| `src/api/eval_routes.py` | 修改 | batch-assess、grade-config 接口 |

### 前端

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `frontend/src/api/client.ts` | 修改 | batchAssess、gradeConfig API 方法 |
| `frontend/src/pages/EvaluationPage.tsx` | 修改 | 雷达图、维度表、批量评价、规则维度列 |
| `frontend/src/components/RadarChart.tsx` | **新增** | SVG 雷达图组件 |
| `frontend/src/components/GradeConfigPanel.tsx` | **新增** | 阈值配置面板 |

### 测试

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `tests/evaluation/test_models.py` | 修改 | 20 个新测试（Dimension, fuzzy_threshold 等） |
| `tests/evaluation/test_fuzzy_scorer.py` | **新增** | 6 个测试 |
| `tests/evaluation/test_rule_engine.py` | 修改 | 适配 float 返回值 |
| `tests/evaluation/test_evaluator.py` | 修改 | 维度分组 + 批量评价测试 |
| `tests/evaluation/test_rsr.py` | **新增** | 6 个测试 |
| `tests/evaluation/test_import_handler.py` | 修改 | 维度解析、标准化、去重测试 |
| `tests/evaluation/test_integration.py` | 修改 | 多维评价、批量 RSR、阈值配置集成测试 |

---

## 测试覆盖

**总计 229 个评价模块测试全部通过**：

| 测试文件 | 数量 | 覆盖范围 |
|---------|------|---------|
| test_models.py | 71 | 数据模型、枚举、字段验证 |
| test_fuzzy_scorer.py | 6 | 模糊评分、缓存、降级 |
| test_rule_engine.py | 19 | 规则匹配（精确+模糊） |
| test_evaluator.py | 24 | 维度分组、批量 RSR、等级映射 |
| test_rsr.py | 6 | RSR 编秩、递阶、动态分档 |
| test_import_handler.py | 30 | 规则提取、标准化、去重 |
| test_integration.py | 13 | 端到端闭环、多维评价、批量评价 |
| 其他（closed_loop, version_manager 等） | 60 | 已有功能回归 |

---

## Git 提交记录

```
dc537a2 test(evaluation): add integration tests for multi-dimensional evaluation, batch RSR, grade config
1760f8c feat(evaluation): Tab 1 radar chart + dimension table + batch assessment + Tab 3 dimension/threshold + GradeConfigPanel
9d3bd3e feat(evaluation): add batch-assess, grade-config frontend API methods
29ffe3f feat(evaluation): add batch-assess, grade-config API endpoints
d56bdc6 feat(evaluation): improve rule extraction with 22-indicator prompt, standardization, dedup
e957517 feat(evaluation): add batch evaluation with RSR hierarchical synthesis
d24b2f1 feat(evaluation): Evaluator with dimension grouping and GradeConfig
678e77a feat(evaluation): add RSR module for rank-sum ratio computation
66ba4df feat(evaluation): add FuzzyScorer with LLM scoring and cache
57a8027 feat(evaluation): add RadarChart SVG component
463626b test(evaluation): add tests for new model fields (Dimension, fuzzy_threshold, etc.)
626fe1a feat(evaluation): extend models with Dimension, fuzzy_threshold, 4-grade system
```

---

## 向后兼容

- 现有规则默认 `dimension=technical`、`fuzzy_threshold=0.6`
- 精确匹配成功时直接返回 1.0（不调用 LLM），行为与 V2.0 一致
- `Grade` 枚举值变更（高/中/低 → 优秀/良好/合格/不可再制造），API 返回新值
- 所有新增字段使用 Optional 或默认值，旧字段不删除

---

## 已知限制

1. **RSR 动态分档在样本量<5 时不稳定**：Probit 回归需要足够样本，2~3 个版本时阈值可能异常
2. **模糊评分依赖 LLM**：每次模糊匹配调用一次 LLM（有缓存），首次评分较慢
3. **去重仅支持 Level 1-2**：精确名称匹配和条件重叠，语义去重（Level 3）未实现
4. **维度权重固定**：默认三维度等权（1/3, 1/3, 1/3），暂不支持自定义维度权重

---

## 后续迭代方向

- [ ] OAT 敏感性分析（单因素扰动测试）
- [ ] 结构熵权法权重优化
- [ ] LLM 语义去重（Level 3）
- [ ] 维度权重用户自定义
- [ ] 批量评价结果导出（CSV/PDF）
