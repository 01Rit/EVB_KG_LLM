# V2.0 — L4 评价层 + 闭环优化系统

**发布日期**: 2026-05-20 | **标签**: `v2.0-l4-evaluation-closed-loop`

---

## 概述

本次迭代在现有 L1（组件层）/L2（文档层）/L3（术语层）三层知识图谱基础上，新增 **L4 评价层**，构建"闭环优化图谱"。核心目标：对动力电池拆卸方案进行可拆卸性评价，并支持设计版本迭代优化——实现"设计→评价→问题发现→改进→新设计→再评价"的工程优化闭环。

从"静态知识图谱"升级为"闭环优化图谱"：

```
DesignVersion V1 (子图快照)
  → EVALUATED_BY → L4_Assessment (评分+等级)
      → EXPLAINED_BY → ReasoningPath (规则匹配链，可复现)
      → REVIEWS → ExpertFeedback (专家确认/修正)
      → IDENTIFIES → OptimizationAction [图操作指令]
            → PRODUCES → DesignVersion V2 (新子图)
                  → EVALUATED_BY → L4_Assessment (再评价)
                        → ...循环直到满意...
```

---

## 架构变更

### 图谱层级

```
V1.5 (三层):
  L1 Component ──→ L2 Document ──→ L3 Term
       │                │
       └────────────────┘ (跨层关系)

V2.0 (四层):
  L1 Component ──→ L2 Document ──→ L3 Term
       │
       └──→ [L4 评价层]
              ├── DesignVersion     (设计版本，子图锚点)
              ├── L4_Rule           (评价规则，图结构条件)
              ├── L4_Assessment     (评价结果，评分+等级)
              ├── ReasoningPath     (推理路径，可复现)
              ├── ExpertFeedback    (专家反馈)
              └── OptimizationAction (优化操作，图操作指令)
```

### 核心设计理念

1. **DesignVersion 是子图快照**，而非 JSON 参数——通过 `CONTAINS` 关系持有真实的组件/连接/工具节点
2. **OptimizationAction 是可执行的图操作指令**（ADD_NODE、REMOVE_NODE、MODIFY_PROPERTY 等），而非文本建议
3. **ReasoningPath 只存稳定知识**，不存储 `llm_prompt`/`llm_response` 等非稳定中间态
4. **确定性评分引擎**——评价计算不依赖 LLM，LLM 仅用于规则提取和自然语言反馈生成

---

## 变更详情

### 1. 数据模型 (`src/evaluation/models.py`, 270 行)

定义了 8 个枚举 + 20 个 Pydantic 模型，覆盖所有 L4 实体：

| 模型 | 说明 |
|------|------|
| `DesignVersion` / `DesignVersionCreate` | 设计版本，子图锚点 |
| `DesignVersionDetail` | 版本详情，含展开的组件/连接列表 |
| `L4Rule` / `L4RuleCreate` / `L4RuleCondition` | 评价规则及图结构条件 |
| `L4Assessment` / `L4AssessmentCreate` | 评价结果（评分+等级+规则匹配明细） |
| `RuleMatchDetail` | 单条规则匹配详情 |
| `ReasoningPath` | 推理路径（匹配链+聚合分数+置信度因子） |
| `ExpertFeedback` / `ExpertFeedbackCreate` | 专家反馈（确认/修正/拒绝） |
| `OptimizationAction` / `OptimizationActionCreate` | 优化操作（图操作指令+载荷） |
| `DesignPredictionRequest` / `DesignPredictionResponse` | 设计预测请求/响应 |
| `RuleExtractRequest` / `CandidateRule` | 规则提取请求/候选规则 |
| `GradeStandard` / `ComparisonRef` / `FeedbackTemplate` | 辅助知识模型 |

**关键枚举**：

| 枚举 | 值 |
|------|-----|
| `Grade` | 高 / 中 / 低 |
| `RuleStatus` | pending_review / active / disabled |
| `VersionStatus` | draft / evaluated / optimized / archived |
| `ActionOperation` | ADD_NODE / REMOVE_NODE / MODIFY_PROPERTY / ADD_REL / REMOVE_REL / SWAP_CONNECTION |

### 2. 规则引擎 (`src/evaluation/rule_engine.py`, 106 行)

- **CRUD 操作**：创建/查询/更新/删除评价规则
- **图模式匹配**：将规则条件映射为子图关系匹配
  - `REQUIRES_CONNECTION` → 匹配子图中的 `USES_CONNECTION` 关系
  - `REQUIRES_TOOL` → 匹配子图中的 `REQUIRES_TOOL` 关系
  - `REQUIRES_STRUCTURE` → 匹配子图中的 `HAS_FEATURE` 关系
  - `CONSTRAINED_BY` → 匹配子图中的 `CONSTRAINED_BY` 关系

### 3. 评价引擎 (`src/evaluation/evaluator.py`, 170 行)

- **确定性加权评分**：`overall_score = Σ(conclusion_score × weight) / Σ(weight)`
- **等级映射**：≥0.7 → 高，≥0.4 → 中，<0.4 → 低
- **推理路径生成**：记录每条规则的匹配状态、得分贡献、匹配模式
- **置信度计算**：规则覆盖率、匹配/未匹配计数

### 4. 版本管理 (`src/evaluation/version_manager.py`, 100 行)

- 创建设计版本（子图快照）
- 版本列表查询（支持按 design_name 过滤）
- 版本详情（展开组件/连接/关系）
- 子图存储与检索
- **版本差异对比**：added/removed/modified 节点和关系

### 5. 操作执行器 (`src/evaluation/action_executor.py`, 139 行)

支持 6 种图操作指令的创建与执行：

| 操作 | 说明 |
|------|------|
| `ADD_NODE` | 添加新节点 |
| `REMOVE_NODE` | 移除节点 |
| `MODIFY_PROPERTY` | 修改节点属性 |
| `ADD_REL` | 添加关系 |
| `REMOVE_REL` | 移除关系 |
| `SWAP_CONNECTION` | 替换连接（移除旧关系+添加新关系） |

**不可变语义**：操作执行采用深拷贝，不修改原始子图。

### 6. 反馈生成器 (`src/evaluation/feedback_generator.py`, 184 行)

- 基于评分结果生成结构化建议
- 识别高风险规则（未匹配 + 高权重）
- 生成改进建议、风险提示、性能对比参照
- 可选 LLM 自然语言生成

### 7. 闭环编排器 (`src/evaluation/closed_loop.py`, 168 行)

统一协调"推理→反馈→修正→更新"四阶段：

| 方法 | 功能 |
|------|------|
| `reason(version_id)` | 执行评价，生成 L4_Assessment |
| `generate_feedback(assessment_id)` | 生成结构化反馈文本 |
| `correct(assessment_id, feedback_data)` | 记录专家修正意见 |
| `generate_actions(assessment_id)` | 基于评价结果生成优化操作 |
| `apply_and_new_version(version_id, action_ids)` | 应用操作→深拷贝子图→创建新版本 |

### 8. 知识导入 (`src/evaluation/import_handler.py`, 130 行)

四重可靠性保障的规则导入流程：

| 阶段 | 机制 | 作用 |
|------|------|------|
| 提取 | LLM 结构化 prompt + JSON schema | 规则格式规范化 |
| 校验 | 规则条件实体必须在 KG 中存在 | 过滤幻觉引用 |
| 审核 | 人工逐条确认才激活 | 最终质量关 |
| 追踪 | 每条规则记录命中率和评分影响 | 运行中持续验证 |

### 9. API 路由 (`src/api/eval_routes.py`, 272 行)

共 **20 个 REST API 端点**：

#### 规则管理 (5)

| 方法 | 路径 | 功能 |
|------|------|------|
| POST | `/api/v1/evaluation/rules` | 创建评价规则 |
| GET | `/api/v1/evaluation/rules` | 查询规则列表（支持 status 过滤） |
| GET | `/api/v1/evaluation/rules/{id}` | 获取单条规则 |
| PUT | `/api/v1/evaluation/rules/{id}` | 更新规则 |
| DELETE | `/api/v1/evaluation/rules/{id}` | 删除规则 |

#### 设计版本管理 (4)

| 方法 | 路径 | 功能 |
|------|------|------|
| POST | `/api/v1/evaluation/versions` | 创建设计版本 |
| GET | `/api/v1/evaluation/versions` | 版本列表（支持 design_name 过滤） |
| GET | `/api/v1/evaluation/versions/{id}` | 版本详情 |
| GET | `/api/v1/evaluation/versions/{v1}/diff/{v2}` | 两版本差异对比 |

#### 评价闭环 (7)

| 方法 | 路径 | 功能 |
|------|------|------|
| POST | `/api/v1/evaluation/assess` | 触发评价 |
| GET | `/api/v1/evaluation/assessments/{id}` | 评价详情 |
| POST | `/api/v1/evaluation/assessments/{id}/feedback` | 专家修正 |
| POST | `/api/v1/evaluation/assessments/{id}/feedback-text` | 生成反馈文本 |
| POST | `/api/v1/evaluation/assessments/{id}/optimize` | 生成优化操作 |
| POST | `/api/v1/evaluation/actions/apply` | 应用操作→新版本 |

#### 设计预测 (1)

| 方法 | 路径 | 功能 |
|------|------|------|
| POST | `/api/v1/evaluation/predict` | 参数化输入预测可拆卸性 |

#### 知识导入 (3)

| 方法 | 路径 | 功能 |
|------|------|------|
| POST | `/api/v1/evaluation/import/extract` | 从 L2 文档提取候选规则 |
| GET | `/api/v1/evaluation/import/candidates` | 待审核规则列表 |
| POST | `/api/v1/evaluation/import/approve/{id}` | 审核通过 |
| POST | `/api/v1/evaluation/import/reject/{id}` | 审核拒绝 |

### 10. 前端页面 (`frontend/src/pages/EvaluationPage.tsx`, 222 行)

新增 **EvaluationPage** 页面，三个 Tab：

| Tab | 功能 |
|-----|------|
| 评价看板 | 选择版本→开始评价→展示评分/等级/规则匹配链/自然语言反馈 |
| 闭环工作台 | 问题列表→专家修正→生成优化建议→版本迭代历史 |
| 规则库管理 | 规则 CRUD + 条件可视化 + 命中率统计 + 知识导入 |

同时修改了：
- `frontend/src/api/client.ts` — 新增 `evaluationApi`，20 个端点方法
- `frontend/src/App.tsx` — 新增 `/evaluation` 路由
- `frontend/src/components/Layout/Sidebar.tsx` — 新增导航入口

---

## 模块结构

```
src/evaluation/          (1267 行，不含 __init__.py)
├── models.py            (270 行)  数据模型：8 枚举 + 20 Pydantic 模型
├── rule_engine.py       (106 行)  规则引擎：CRUD + 图模式匹配
├── evaluator.py         (170 行)  评价引擎：加权评分 + 推理路径
├── version_manager.py   (100 行)  版本管理：子图快照 + 差异对比
├── action_executor.py   (139 行)  操作执行：6 种图操作指令
├── feedback_generator.py(184 行)  反馈生成：结构化建议 + LLM
├── closed_loop.py       (168 行)  闭环编排：推理→反馈→修正→更新
└── import_handler.py    (130 行)  知识导入：LLM 提取 + 一致性校验

src/api/eval_routes.py   (272 行)  API 路由：20 个 REST 端点

frontend/src/pages/EvaluationPage.tsx (222 行)  前端页面：3 个 Tab

tests/evaluation/        (2974 行)  单元测试：9 个文件
tests/api/test_eval_routes.py       API 路由测试
```

---

## 测试验证

共 **178 个测试全部通过**：

| 测试文件 | 测试数 | 覆盖内容 |
|---------|--------|---------|
| `test_models.py` | 50 | 数据模型验证、枚举、序列化 |
| `test_rule_engine.py` | 19 | CRUD + 图模式匹配 |
| `test_evaluator.py` | 13 | 加权评分 + 等级映射 + 推理路径 |
| `test_version_manager.py` | 13 | 版本创建/查询 + 子图存储 + 差异对比 |
| `test_action_executor.py` | 15 | 6 种操作指令创建与执行 |
| `test_feedback_generator.py` | 12 | 反馈生成 + 风险识别 |
| `test_closed_loop.py` | 20 | 闭环四阶段流程 |
| `test_import_handler.py` | 12 | 规则提取 + 一致性校验 |
| `test_integration.py` | 6 | 端到端集成测试 |
| `test_eval_routes.py` | 18 | API 路由 + 预测端点 |

运行方式：
```bash
python -m pytest tests/evaluation/ tests/api/test_eval_routes.py -v
```

---

## 性能指标

| 指标 | V1.5 | V2.0 |
|------|------|------|
| 图谱层级 | L1/L2/L3 三层 | L1/L2/L3/**L4** 四层 |
| 评价能力 | 无 | 确定性加权评分 + 推理路径 |
| 设计版本 | 无 | 子图快照 + 版本对比 |
| 闭环优化 | 无 | 推理→反馈→修正→更新 |
| 规则管理 | 无 | CRUD + 图模式匹配 + LLM 导入 |
| 设计预测 | 无 | 参数化输入预测可拆卸性 |
| 后端代码量 | ~5000 行 | ~6300 行 (+1267) |
| 测试用例 | ~140 | ~320 (+178) |
| API 端点 | ~15 | ~35 (+20) |
| 前端页面 | 5 页 | 6 页 (+EvaluationPage) |

---

## 新增/修改文件清单

### 新增文件

| 文件 | 行数 | 说明 |
|------|------|------|
| `src/evaluation/__init__.py` | 0 | 模块初始化 |
| `src/evaluation/models.py` | 270 | L4 数据模型 |
| `src/evaluation/rule_engine.py` | 106 | 规则引擎 |
| `src/evaluation/evaluator.py` | 170 | 评价引擎 |
| `src/evaluation/version_manager.py` | 100 | 版本管理 |
| `src/evaluation/action_executor.py` | 139 | 操作执行器 |
| `src/evaluation/feedback_generator.py` | 184 | 反馈生成器 |
| `src/evaluation/closed_loop.py` | 168 | 闭环编排器 |
| `src/evaluation/import_handler.py` | 130 | 知识导入 |
| `src/api/eval_routes.py` | 272 | API 路由（20 端点） |
| `frontend/src/pages/EvaluationPage.tsx` | 222 | 前端评价页面 |
| `tests/evaluation/test_models.py` | - | 模型测试 |
| `tests/evaluation/test_rule_engine.py` | - | 规则引擎测试 |
| `tests/evaluation/test_evaluator.py` | - | 评价引擎测试 |
| `tests/evaluation/test_version_manager.py` | - | 版本管理测试 |
| `tests/evaluation/test_action_executor.py` | - | 操作执行器测试 |
| `tests/evaluation/test_feedback_generator.py` | - | 反馈生成测试 |
| `tests/evaluation/test_closed_loop.py` | - | 闭环编排测试 |
| `tests/evaluation/test_import_handler.py` | - | 知识导入测试 |
| `tests/evaluation/test_integration.py` | - | 集成测试 |
| `tests/api/test_eval_routes.py` | - | API 路由测试 |
| `docs/superpowers/specs/2026-05-20-l4-evaluation-closed-loop-design.md` | - | 设计文档 |
| `docs/superpowers/plans/2026-05-20-l4-evaluation-closed-loop.md` | - | 实现计划 |

### 修改文件

| 文件 | 变更 |
|------|------|
| `src/main.py` | 注册 eval_router |
| `frontend/src/App.tsx` | 新增 /evaluation 路由 |
| `frontend/src/api/client.ts` | 新增 evaluationApi（20 个方法） |
| `frontend/src/components/Layout/Sidebar.tsx` | 新增评价导航入口 |
| `docker-compose.yml` | 版本号更新为 2.0 |

---

## 环境依赖

本版本无新增外部依赖，复用已有：
- Python 3.10+
- FastAPI + Pydantic V2
- Neo4j 5.20
- OpenAI-compatible LLM API（DeepSeek）
- React 18 + TypeScript + Vite

---

## Docker 部署

```bash
# 构建并启动
docker-compose up -d --build

# 验证后端健康
curl http://localhost:50060/api/v1/health

# 验证评价 API
curl http://localhost:50060/api/v1/evaluation/rules
```

前端访问：`http://localhost:3090/evaluation`

---

## 已知限制与后续计划

1. **闭环工作台** Tab 2 和**规则库管理** Tab 3 前端目前为占位实现，待后续完善
2. 知识导入依赖 LLM 调用，需配置有效的 `OPENAI_API_KEY`
3. 版本管理和评价数据目前存储在内存中，重启后丢失；后续可迁移至 Neo4j 持久化
4. 规则匹配目前为精确标签匹配，后续可扩展为模糊语义匹配
